import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
quantitySelector: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 10,
  },
  quantityButton: {
    width: 60,
    height: 60,
    borderRadius: 100,
  },
 
  quantityText: {
    fontSize: 20,
    lineHeight: 24,
    fontWeight: '800',
    marginHorizontal: 10,
  },
})